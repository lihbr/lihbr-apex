---javascript
{
	layout: "default",
	pagination: {
		data: "prismic.page",
		size: 1,
		alias: "document",
		addAllPagesToCollections: true,
		before: (data) => data.map((doc) => doc.uid === "home" ? { ...doc, uid: "" } : doc),
	},
	permalink: "{{ document.uid }}/",
}
---

<h1>{% asText document.data.title %}</h1>

