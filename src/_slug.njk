---javascript
{
	layout: "default",
	pagination: {
		data: "prismic.page",
		size: 1,
		alias: "document",
		addAllPagesToCollections: true,
		before: (data) => data.map((doc) => doc.uid === "home" ? { ...doc, uid: "index" } : doc),
	},
	permalink: {
		build: "{{ document.uid }}.html",
		preview: "/preview/:uid",
	},
}
---

{# This file is just sort of a mixin to generate every `page` documents. #}
{# Relevant templates are available within their matching `:slug.njk` files. #}

{%- if (document.uid === "index") -%}

{%- include "./_includes/pages/index.njk" -%}

{%- elif (document.uid === "post") -%}

post

{%- elif (document.uid === "art") -%}

art

{%- elif (document.uid === "about") -%}

about

{%- elif (document.uid === "contact") -%}

contact

{%- endif -%}
